# 보안 기본 개념

## 한 줄 요약

정보보안은 허가된 사용자가 허가된 정보와 기능에 적절하게 접근할 수 있도록 보호하는 활동이다.

## CIA 3요소

### 기밀성 (Confidentiality)
허가되지 않은 사용자가 정보를 보지 못하도록 보호하는 성질이다.

대표적인 보호 수단:
- 암호화
- 접근통제
- 인증
- VPN

정보가 유출되거나 몰래 열람되면 기밀성이 침해된 것이다.

### 무결성 (Integrity)
정보가 허가 없이 변경되거나 훼손되지 않았음을 보장하는 성질이다.

대표적인 보호 수단:
- 해시
- 전자서명
- MAC(Message Authentication Code)

데이터가 몰래 수정되면 무결성이 침해된 것이다.

### 가용성 (Availability)
필요한 사용자가 필요할 때 시스템이나 정보를 사용할 수 있도록 보장하는 성질이다.

대표적인 위협:
- DoS / DDoS
- 시스템 장애
- 자원 고갈

## 인증과 인가

### 인증 (Authentication)
사용자가 누구인지 확인하는 과정이다.

예: ID/PW, OTP, 인증서

### 인가 (Authorization)
인증된 사용자가 특정 자원이나 기능을 사용할 권한이 있는지 확인하는 과정이다.

정리:
- 인증 = 너 누구야?
- 인가 = 너 이거 해도 돼?

## 보안 문제를 보는 기본 흐름

보안 개념은 다음 흐름으로 보면 이해하기 쉽다.

`자산 → 위협 → 취약점 → 공격 → 침해 → 보안 대책`

예를 들어 웹 애플리케이션에서 입력값 검증이 부족하면 SQL Injection 공격에 악용될 수 있고, 그 결과 DB 정보가 유출될 수 있다. 이때 Prepared Statement와 같은 방식이 대응책이 된다.

## 시험 포인트

- 기밀성: 정보의 비인가 열람 방지
- 무결성: 비인가 변경 방지
- 가용성: 정상적인 서비스 이용 보장
- 인증과 인가를 구분할 것
- MAC은 문맥에 따라 MAC Address와 Message Authentication Code가 다르므로 주의

## 실무 연결

서비스 장애나 DDoS는 단순 운영 문제뿐 아니라 가용성 침해로도 볼 수 있다. 모니터링과 장애 대응 역시 보안의 가용성 측면과 연결된다.
